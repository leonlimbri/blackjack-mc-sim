INIT_CARDS=["J","Q","K","A"]+[str(c) for c in range(2, 11)]
INIT_CARDS_COUNT={c: 4 for c in INIT_CARDS}

class Cards:
    def __ini

class Blackjack:
    def __init__(self, curr_card_count: Cards):
        curr_card_count=curr_card_count if curr_card_count else INIT_CARDS_COUNT
        player_hand, dealer_hand=[], []

    def play():
